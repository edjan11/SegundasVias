{
  "name": "segundas-vias",
  "version": "0.1.0",
  "description": "Painel local para emissao de 2 vias (JSON/XML) com atalho na bandeja.",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "serve": "http-server . -p 4178 -c-1",
    "dev": "npm-run-all --parallel watch serve"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.3"
  }
}
